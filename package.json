{
  "name": "coding-test",
  "version": "0.0.0",
  "private": true,
  "author": "Sean Li <sohey33@gmail.com>",
  "license": "MIT",
  "scripts": {
    "install": "cd client && yarn install && cd ../server && yarn install",
    "server-start": "cd server && yarn start",
    "server-test": "cd server && yarn test",
    "server-watch": "cd server && yarn watch",
    "server-coverage": "cd server && yarn coverage",
    "server": "cd server && yarn server",
    "client-build": "cd client && yarn build",
    "client-start": "cd client && yarn start",
    "client-test": "cd client && yarn test",
    "client-coverage": "cd client && yarn coverage"
  }
}
