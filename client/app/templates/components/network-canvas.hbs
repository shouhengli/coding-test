{{#visjs-network options=networkOptions}}
	{{#each users as |user|}}
		{{visjs-node nId=user.id label=user.id}}
	{{/each}}
	{{#each relations as |rel|}}
		{{visjs-edge from=rel.from to=rel.to arrows="to"}}
	{{/each}}
{{/visjs-network}}
